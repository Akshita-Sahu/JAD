<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="JAD_LOG_PATH" value="${JAD_LOG_PATH:-${user.home}/logs/jad}" />
    <property name="JAD_LOG_FILE" value="${JAD_LOG_FILE:-${JAD_LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}}/jad.log}" />

    <property name="RESULT_LOG_FILE" value="${RESULT_LOG_FILE:-${JAD_LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}}/../jad-cache/result.log}" />

    <!-- jad.log -->
    <appender name="JAD" class="com.akshita.jad.deps.ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${JAD_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} -%msg%n</pattern>
        </encoder>
        <rollingPolicy class="com.akshita.jad.deps.ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${JAD_LOG_FILE}.%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <maxHistory>7</maxHistory>
            <maxFileSize>1MB</maxFileSize>
            <totalSizeCap>10MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- result.log -->
    <appender name="RESULT" class="com.akshita.jad.deps.ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${RESULT_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} -%msg%n</pattern>
        </encoder>
        <rollingPolicy class="com.akshita.jad.deps.ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${RESULT_LOG_FILE}.%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <maxHistory>7</maxHistory>
            <maxFileSize>1MB</maxFileSize>
            <totalSizeCap>10MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="result" level="INFO" additivity="false">
        <appender-ref ref="RESULT" />
    </logger>

    <root level="INFO">
        <appender-ref ref="JAD" />
    </root>

</configuration>