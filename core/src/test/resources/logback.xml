<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<property name="JAD_LOG_PATH"
		value="${JAD_LOG_PATH:-${user.home}/logs/jad}" />
	<property name="JAD_LOG_FILE"
		value="${JAD_LOG_FILE:-${JAD_LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}}/jad.log}" />

	<appender name="APPLICATION"
		class="com.akshita.jad.deps.ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${JAD_LOG_FILE}</file>
		<encoder>
			<pattern>%date %level [%thread] %logger{10} [%file:%line] %msg%n
			</pattern>
		</encoder>
		<rollingPolicy
			class="com.akshita.jad.deps.ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<fileNamePattern>${JAD_LOG_FILE}.%d{yyyy-MM-dd}.%i.log
			</fileNamePattern>
			<maxHistory>7</maxHistory>
			<maxFileSize>1MB</maxFileSize>
			<totalSizeCap>10MB</totalSizeCap>
		</rollingPolicy>
	</appender>

	<appender name="CONSOLE"
		class="com.akshita.jad.deps.ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>${CONSOLE_LOG_PATTERN}</pattern>
			<charset>utf8</charset>
		</encoder>
	</appender>

	<root level="ERROR">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="APPLICATION" />
	</root>

</configuration>