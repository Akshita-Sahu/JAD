---
name: jad-cpu-high
description:  JVM /  CPU （ + ）
---

# JVM CPU （JAD）

： CPU 、、。

：「」，「/」。

## 1)  JVM （）

-  `dashboard`  CPU//GC （）：
  - ：CPU、、GC /。

## 2) （）

-  `thread`  N ：
  - ：`topN=3`  `topN=5`
-  `threadId` （ `take_notes` ）。

：
-  `java.util.regex`、JSON 、： CPU 。
- ： `BLOCKED`， `thread(blocking=true)` 。

## 3) （、）

「」：
-  `stack` / `trace` （）。
- /， `watch`  `tt`，，。

## 4) 

：
- ：dashboard  + topN 
- ：CPU （//GC/）
- ： trace/watch （+），/

