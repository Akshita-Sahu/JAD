#  Java Agent 

 JAD  attach ，`3.2.0`，JAD  java agent 。

 jad zip ， `-javaagent` `jad-agent.jar`：

```
java -javaagent:/tmp/test/jad-agent.jar -jar math-game.jar
```

`jad.properties`。：[JAD Properties](jad-properties.md)

Java Agent ： https://docs.oracle.com/javase/8/docs/api/java/lang/instrument/package-summary.html
