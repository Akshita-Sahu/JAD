# JAD Install

## 

### `jad-boot`（）

`jad-boot.jar`，`java -jar`：

```bash
curl -O https://github.com/Akshita-Sahu/JAD/jad-boot.jar
java -jar jad-boot.jar
```

：

```bash
java -jar jad-boot.jar -h
```

- ， akshita-sahu ：

  ```bash
  java -jar jad-boot.jar --repo-mirror akshita-sahu --use-http
  ```

### `jad.sh`

JAD  Linux/Unix/Mac ，，， `` ：

```bash
curl -L https://github.com/Akshita-Sahu/JAD/install.sh | sh
```

 `jad.sh` ， `$PATH` 。

 shell `./jad.sh`，。

`./jad.sh -h`。

## 

，：[![](https://img.shields.io/maven-central/v/com.akshita.jad/jad-packaging.svg?style=flat-square "JAD")](https://github.com/Akshita-Sahu/JAD/download/latest_version?mirror=akshita-sahu)

，`jad-boot.jar`，`java -jar`：

```bash
java -jar jad-boot.jar
```

：

```bash
java -jar jad-boot.jar -h
```

## 

[](manual-install.md)

##  rpm/deb 

 releases  rpm/deb ： https://github.com/akshita-sahu/jad/releases

###  deb

```bash
sudo dpkg -i jad*.deb
```

###  rpm

```bash
sudo rpm -i jad*.rpm
```

### deb/rpm 

，：

```bash
jad.sh
```

##  Cloud Toolkit  JAD

- [ Cloud Toolkit  JAD ](https://github.com/akshita-sahu/jad/issues/570)

## 

：[![](https://img.shields.io/maven-central/v/com.akshita.jad/jad-packaging.svg?style=flat-square "JAD")](https://github.com/Akshita-Sahu/JAD/download/doc/latest_version?mirror=akshita-sahu)

## 

-  Linux/Unix/Mac 

  ：

  ```bash
  rm -rf ~/.jad/
  rm -rf ~/logs/jad
  ```

- Windows  user home `.jad``logs/jad`

---

::: warning
 jdk 6/7 ，[ jad 3](https://github.com/Akshita-Sahu/JAD/3.x/doc/install-detail.html)。
:::
