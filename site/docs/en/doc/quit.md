# quit

exit the current JAD client without affecting other clients. equals **exit**、**logout**、**q** command.

::: tip
just exit JAD client,it means JAD server is not closed,so the changes you do will not be reseted.
:::
